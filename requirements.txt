# ============================================================
# Perry's Wan 2.2 ComfyUI Pipeline — Python Requirements
# Tested on: NVIDIA A100-SXM4-80GB | CUDA 12.6 | Driver 535.183.06
# Python: 3.10.x
# ============================================================
# Install with:
#   Step 1 — PyTorch (requires special index URL, NOT listed below):
#     pip install torch==2.5.1+cu124 torchvision==0.20.1+cu124 torchaudio==2.5.1+cu124 \
#         --index-url https://download.pytorch.org/whl/cu124
#   Step 2 — All other dependencies:
#     pip install -r requirements.txt
# ============================================================

# --- Core ML / PyTorch ecosystem ---
# NOTE: PyTorch is NOT listed here because it needs the special index URL.
# See setup_comfyui.sh or the README for the correct install command.
# torch==2.5.1+cu124
# torchvision==0.20.1+cu124
# torchaudio==2.5.1+cu124

# --- Transformers & model loading ---
transformers==4.47.1
accelerate==1.2.1
diffusers==0.32.1
safetensors==0.4.5
huggingface_hub==0.27.0
tokenizers==0.21.0
sentencepiece==0.2.0

# --- ComfyUI core dependencies ---
einops==0.8.0
omegaconf==2.3.0
pydantic==2.10.4
aiohttp==3.11.11
yarl==1.18.3
aiosignal==1.3.2
frozenlist==1.5.0
attrs==24.3.0
multidict==6.1.0

# --- Image processing ---
pillow==11.1.0
numpy==1.26.4
scipy==1.14.1
scikit-image==0.24.0
imageio==2.36.1
imageio-ffmpeg==0.5.1
opencv-python-headless==4.10.0.84

# --- Video processing (VideoHelperSuite) ---
av==13.1.0
ffmpeg-python==0.2.0

# --- Prompt-enhancement stage client (MasterKey vision-language node) ---
google-generativeai==0.8.3
google-ai-generativelanguage==0.6.15
google-api-core==2.24.0
google-auth==2.37.0
googleapis-common-protos==1.66.0
protobuf==5.29.2
grpcio==1.68.1
grpcio-status==1.68.1

# --- Networking & web server ---
requests==2.32.3
urllib3==2.3.0
certifi==2024.12.14
charset-normalizer==3.4.1
idna==3.10
websockets==14.1

# --- Utilities ---
tqdm==4.67.1
psutil==6.1.0
filelock==3.16.1
packaging==24.2
regex==2024.11.6
rich==13.9.4
typing_extensions==4.12.2
click==8.1.8

# --- Optional: for verify_setup.py checks ---
GPUtil==1.4.0
